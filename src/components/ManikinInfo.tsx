import React from "react";
import Card from "@material-ui/core/Card"
import CardContent from "@material-ui/core/CardContent"

export const ManikinInfo: React.FC = () => {
    return (
        <Card className="ManikinInfo">
            <CardContent>some info should go here in theory, right?  Like just a short little description paragraph or whatever, I don't really care.</CardContent>
        </Card>)
}